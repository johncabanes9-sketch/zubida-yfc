node_modules/
.next/
out/
.env*
!.env.example
*.log
.DS_Store
.vercel
next-env.d.ts

# Incremental build cache. Regenerated by `tsc`, so it shows up modified after
# every type check and has been noise in `git status` all through this branch.
tsconfig.tsbuildinfo

# Crash-recovery snapshot written by prove:editor before it touches published
# content. Present only while the suite is mid-run or after it failed.
scripts/.editor-snapshot.json
